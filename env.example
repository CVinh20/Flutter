# Database
MONGODB_URI=mongodb://localhost:27017/grooming_db
MONGODB_URI=mongodb+srv://congvinh:WBxmTwbGE2lgK4gM@lehuynhcongvinh.augbndo.mongodb.net/grooming_db?retryWrites=true&w=majority

# Server
PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRE=7d

# CORS
CORS_ORIGIN=http://localhost:3000
# Email Configuration (SMTP)
# Để gửi email thực sự, cần cấu hình các giá trị sau:

# Cho Gmail:
# 1. Bật "2-Step Verification" trong tài khoản Google
# 2. Tạo "App Password" tại: https://myaccount.google.com/apppasswords
# 3. Sử dụng App Password làm SMTP_PASS (không dùng mật khẩu thường)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password-here

# Application Settings
APP_NAME=Gentlemen Grooming
FRONTEND_URL=http://localhost:3000